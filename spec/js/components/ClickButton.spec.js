import {
    ClickButton
} from '../../../src/js/component/ClickButton.js'
describe('ClickButton.js is a component generator that creates a \`button\` element and wires it to a \`ClickCounter\` object.', () => {
    let underTest;
    beforeEach(() => underTest = new ClickButton())
});